/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Dataasset from './Dataasset';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Dataasset);
